# INVESTRA synthetic RAG dataset

This directory contains only synthetic, non-sensitive development material. Add
approved TXT, PDF, or DOCX files through the authenticated evidence endpoint;
the ingestion pipeline chunks and indexes each accepted document without a full
application rebuild. Do not place real operational records in this repository.
